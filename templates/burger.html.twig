{% extends 'base.html.twig' %}
{% block title %}Liste des burgers{% endblock %}

{% block body %}Liste des burgers
<ul>
        {% for number in tableau_burger %}
           <li><a href="{{ path('burger_id', {'id': number}) }}">{{ number }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
